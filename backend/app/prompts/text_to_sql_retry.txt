Your previous SQL failed validation.

【DATABASE CONTEXT】

{database_context}

【ALLOWED TABLES】

{allowed_tables_block}

【MAX ROWS】

{max_rows}

【USER QUESTION】

{question}

【PREVIOUS SQL】

{previous_sql}

【VALIDATION ERRORS】

{validation_errors}

Generate a corrected SQL query that fixes ALL the validation
errors above. Follow every rule: read-only SELECT, single
statement, only allowed tables, columns from the database
context only, integer LIMIT not exceeding {max_rows}.

Return the SQL query only. No explanation, no markdown fences,
no comments.
