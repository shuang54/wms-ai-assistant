# WMS AI Assistant - Phase 1 Dependencies
# Only the dependencies actually required by Phase 1.
# LangChain / LangGraph / pgvector / vector DBs are NOT introduced in Phase 1.

# Web framework
fastapi>=0.115.0,<1.0

# ASGI server
uvicorn[standard]>=0.30.0,<1.0

# Data validation
pydantic>=2.7.0,<3.0

# Environment variable loader
python-dotenv>=1.0.0,<2.0

# HTTP client (for future WMS/ERP API calls and tests)
httpx>=0.27.0,<2.0

# Testing
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
